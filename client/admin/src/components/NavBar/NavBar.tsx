import React from 'react';
import { StyledNavBar, StyledTitle } from './NavBar.styles';

export const NavBar = () => {
  return (
    <StyledNavBar>
      <StyledTitle>ThankyouFor Admin</StyledTitle>
    </StyledNavBar>
  );
};
